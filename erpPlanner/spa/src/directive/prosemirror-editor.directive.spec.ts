import { ProsemirrorEditorDirective } from './prosemirror-editor.directive';

describe('ProsemirrorEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProsemirrorEditorDirective();
    expect(directive).toBeTruthy();
  });
});
